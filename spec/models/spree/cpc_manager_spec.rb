require 'spec_helper'

describe Spree::CpcManager do
  context '#set_variant_cpc' do

    before(:each) do
      # Create a variant
      # Set preferences
    end

    it 'should not set the max_cpc lower than the preferred minimum' do

    end

    it 'should not set the max_cpc higher than the preferred maximum' do

    end
  end
end